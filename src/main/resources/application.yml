spring:
  application:
  name: safe-escape-backend

  datasource:
    url: jdbc:mysql://localhost:3306/escape?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update   # create, update, validate, none ? ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  server:
    port: 8080

jwt:
  secret: 12341234123412341234123412341234
  token-validity-in-milliseconds: 3600000  # 1시간
